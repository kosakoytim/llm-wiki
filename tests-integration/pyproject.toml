[project]
name = "llm-wiki-integration-tests"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "mcp>=1.0",
]

[tool.pytest.ini_options]
testpaths = ["."]
asyncio_mode = "auto"
