services:
  llm-wiki:
    build:
      context: .
      args:
        VERSION: v0.4.1
    ports:
      - "8080:8080"
    volumes:
      - ./wiki-data:/wiki/data
    restart: unless-stopped
    environment:
      - RUST_LOG=info
